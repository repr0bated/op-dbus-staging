//! Handler modules for centralized HTTP server
//!
//! Each MCP service gets its own handler module with consistent patterns.

pub mod chat;

// Re-export main handlers for easy access
pub use chat::*;


//!
//! Each MCP service gets its own handler module with consistent patterns.

pub mod chat;

// Re-export main handlers for easy access
pub use chat::*;


